import { describe, it } from 'vitest'

// lib/tabs.ts 的功能已迁移至 lib/vault.ts,对应测试见 tests/lib/vault.test.ts
describe('lib/tabs (migrated)', () => {
  it('archive and switch logic is covered in vault.test.ts', () => {
    // intentional no-op placeholder
  })
})
